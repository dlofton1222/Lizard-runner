{
  "name": "lizard-runner-capacitor",
  "version": "1.1.0",
  "private": true,
  "description": "Lizard Runner \u2014 Android via Capacitor with embedded jump audio",
  "scripts": {
    "build": "echo 'Using static www; nothing to build'",
    "sync": "npx cap sync android",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "6.1.2"
  }
}